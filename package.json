{
  "name": "brunch-sha",
  "version": "0.1.0",
  "description": "Adds a sha sum file after compiling.",
  "author": "Jeremy Mack @mutewinter",
  "homepage": "https://github.com/mutewinter/brunch-sha",
  "repository": {
    "type": "git",
    "url": "git@github.com:mutewinter/brunch-sha.git"
  },
  "main": "./lib/index",
  "scripts": {
    "prepublish": "rm -rf lib && coffee --bare --output lib/ src/",
    "test": "node_modules/.bin/mocha --require test/common.js"
  },
  "dependencies": { },
  "devDependencies": {
    "mocha": "1.11.0",
    "chai": "1.7.0"
  }
}
